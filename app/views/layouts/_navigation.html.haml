= link_to "Ask a Question", "http://doctalk.herokuapp.com/voice/init_call"

/ = link_to "Applepie", root_path, :class => 'brand'
/ %ul.nav
/   - if user_signed_in?
/     %li
/       = link_to 'Logout', destroy_user_session_path, :method=>'delete'
/   - else
/     %li
/       = link_to 'Login', new_user_session_path
/   - if user_signed_in?
/     %li
/       = link_to 'Edit account', edit_user_registration_path
/   - else
/     %li
/       = link_to 'Sign up', new_user_registration_path

%nav.top-bar
  %ul
    %li.name
      %h1
        = link_to "DocTalk", root_path, :class => 'brand'
    %li
      %a{:href => "#"}
        %i.foundicon-search
    %li
      = link_to "Ask", new_question_path, :id => 'ask'
    - unless current_user.nil?
      %li.toggle-topbar
        %a{:href => "#"}

  %section
    %ul.right
      %li
        - unless current_user.nil?
          = link_to "#{current_user.name}", user_path(current_user.id.to_s)
      - unless current_user.nil?
        %li
          = link_to 'Edit account', edit_user_registration_path
      %li
        - unless current_user.nil?
          = link_to 'Logout', destroy_user_session_path, :method=>'delete'
